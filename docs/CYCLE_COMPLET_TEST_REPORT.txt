Asset Factory - validation de la refonte des sorties et du cycle complet

TESTS EXECUTES DANS L'ENVIRONNEMENT DE LIVRAISON
Python : 3.13.5
Commande : python3 -B -m pytest -q

102 tests réussis, 13 sous-tests réussis.

COUVERTURE PRINCIPALE
- structure versionnée outputs/assets/<AssetId>/vNNN ;
- conservation des générations précédentes ;
- sélection TRELLIS / TripoSR ;
- normalisation Blender simulée ;
- import Unreal simulé ;
- versionnement Unreal et incrément automatique d'une version déjà occupée ;
- chargement hors ligne TRELLIS ;
- nommage PNG/GLB ;
- contrats statiques PowerShell et profils génériques.

NON EXECUTE DANS CET ENVIRONNEMENT
- exécution PowerShell réelle : pwsh/powershell n'est pas installé ici ;
- tests/test-cycle-contracts.ps1 : fourni pour la machine Windows cible ;
- inférence réelle ComfyUI, TRELLIS ou TripoSR ;
- exécution réelle de Blender ;
- import réel avec UnrealEditor-Cmd.

Les tests Python utilisent des simulations lorsque Blender, Unreal ou les moteurs GPU sont nécessaires.
